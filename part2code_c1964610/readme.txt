Using tools and environment:
python3.7
jupyter
numpy
pandas
gensim
tqdm
Before running, make sure the installation environment is correct and the program is in the same directory as the IMDb folder

Start jupyter: type jupyter notebook in terminal and run

Note: If your operating environment is Windows, make sure your computer's default encoding format is UTF-8.